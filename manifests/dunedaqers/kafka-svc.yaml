---
apiVersion: v1
kind: Service
metadata:
  name: kafka-svc
  labels:
    app: kafka-app
  namespace: kafka-kraft
spec:
  type: NodePort
  ports:
  - port: 9092
    protocol: TCP
    targetPort: kafka-svc
    nodePort: 32666
  selector:
    app: kafka-app
  sessionAffinity: None
