apiVersion: v1

kind: PersistentVolumeClaim
metadata:
  name: postgresql-pv-volume
  namespace: ers
  labels:
    type: local
spec:
  storageClassName: ""
  accessModes:
    - ReadWriteOnce 
  volumeName: mongodb-data
  resources:
    requests:
      storage: 1Gi
