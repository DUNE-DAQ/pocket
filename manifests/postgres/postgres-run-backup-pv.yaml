---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: "postgresql-run-pv-backup"
  namespace: microservices
  labels:
    type: local
spec:
  storageClassName: manual
  accessModes: ["ReadWriteOnce"]
  capacity:
    storage: 2Gi
  hostPath:
    path: '/scratch/kube/postgres-backups/run'
  claimRef:
    name: "postgresql-run-pv-backup-claim"
    namespace: microservices
---
