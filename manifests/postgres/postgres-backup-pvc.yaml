apiVersion: v1

kind: PersistentVolumeClaim
metadata:
  name: "postgresql-pv-backup-claim"
  namespace: ers
  labels:
    type: local
spec:
  storageClassName: ""
  accessModes:
    - ReadWriteOnce 
  volumeName: "postgresql-pv-backup"
  resources:
    requests:
      storage: 1Gi
